enum ParticleMode { attract, repel, orbit, vortex, magnetic }

enum ParticleShape { circle, square, triangle, hexagon, star }

enum PaletteType { cyan, sunset, violet, flame, arctic, lime }

enum FormationType { dna, galaxy, atom, torus }
